import { forwardRef } from "react";
import type { VisualTreeTableProps } from "./types";

const VisualTreeTable = forwardRef((props: VisualTreeTableProps, ref) => {
	return <div></div>;
});

export default VisualTreeTable;
